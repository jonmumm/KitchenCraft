export const MediaRow = ({
  previewMediaIds,
}: {
  previewMediaIds: string[];
}) => {
  console.log({ previewMediaIds });
  //   const Content = async () => {
  //     await kv.hgetall(`recipe:${slug}:media`);
  //     return <></>;
  //   };

  return <></>;
};
